<Window x:Class="Nasa.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Nasa"
        xmlns:Views="clr-namespace:Nasa.Views"
        mc:Ignorable="d"
        
        Title="Nasa" Height="450" Width="800">
    <Window.Resources>
        <DataTemplate DataType="{x:Type local:MainViewModel}">
            <Views:MainWindow/>
        </DataTemplate>

        <DataTemplate DataType="{x:Type local:CuriosityViewModel}>
            <Views:Curiosity/>
        </DataTemplate>"

        <DataTemplate DataType="{x:Type local:OpporunityViewModel}>
            <Views:Opportunity/>
        </DataTemplate>

        <DataTemplate DataType="{x:Type local:SpiritViewModel} >
            <Views:Spirit/>
        </DataTemplate>

    </Window.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="200"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <Button Grid.Row="0" Grid.Column="0" Content="Curiosity Rover" />
        <Button Grid.Row="1" Grid.Column="0" Content="Opportunity Rover"/>
        <Button Grid.Row="2" Grid.Column="0" Content="Spririt Rover"/>
    </Grid>
</Window>
