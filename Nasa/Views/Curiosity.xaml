<UserControl x:Class="Nasa.Views.Curiosity"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:Nasa"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800">


    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="200"/>
        </Grid.ColumnDefinitions>
        <TextBlock  Grid.Column="0" Text="Curiosity" HorizontalAlignment="Center"/>
        <Image Source="{Binding Image, Mode=TwoWay}"  Grid.Column="0" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" Stretch="Fill"/>
        <Button  Grid.Column="1" Content="see" HorizontalAlignment="Center" Command="{Binding SeePhotos}" VerticalAlignment="Center" Width="200" Height="100"/>
    </Grid>
</UserControl>
